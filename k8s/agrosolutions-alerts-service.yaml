apiVersion: v1
kind: Service
metadata:
  name: agrosolutions-alerts-functions
spec:
  type: NodePort
  selector:
    app: agrosolutions-alerts-functions
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
