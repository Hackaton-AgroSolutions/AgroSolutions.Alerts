apiVersion: v1
kind: Service
metadata:
  name: agrosolutions-azurefunction-functions
spec:
  type: NodePort
  selector:
    app: agrosolutions-azurefunction-functions
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
